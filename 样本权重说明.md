# 样本权重功能说明

## 功能概述

本项目现在支持基于`price_diff`绝对值的样本权重计算，允许在模型训练时给予`price_diff`绝对值较大的样本更高的权重。这有助于模型更关注那些价格差异较大的重要样本。

## 功能特点

- ✅ **可选启用**：可以通过配置文件灵活控制是否启用样本权重
- ✅ **双重模式**：支持线性权重和非线性权重两种计算方式
- ✅ **基于price_diff**：权重计算基于`price_diff`的绝对值，而非标签值
- ✅ **完全集成**：权重自动应用于超参数搜索和最终模型训练

## 配置说明

在配置文件（如`config.yaml`）中添加或修改`sample_weight`部分：

```yaml
sample_weight:
  enabled: true  # 是否启用样本权重（true/false）
  weight_type: 'nonlinear'  # 权重类型：'linear' 或 'nonlinear'
  eps: 1.0e-6  # 避免零的小常数
  alpha: 2  # 非线性权重指数（仅在weight_type='nonlinear'时使用）
```

### 参数说明

1. **enabled** (布尔值)
   - `true`: 启用样本权重
   - `false`: 禁用样本权重，所有样本权重相同

2. **weight_type** (字符串)
   - `'linear'`: 线性权重
     - 公式: `weight = |price_diff| + eps`
     - 特点: 权重随`price_diff`绝对值线性增长
   - `'nonlinear'`: 非线性权重
     - 公式: `weight = (|price_diff| + eps) ^ alpha`
     - 特点: 权重随`price_diff`绝对值非线性增长，增长速度由`alpha`控制

3. **eps** (浮点数)
   - 避免零除的小常数
   - 默认值: `1e-6`
   - 确保即使`price_diff`为0时也有最小权重

4. **alpha** (浮点数)
   - 非线性权重的指数参数
   - 仅在`weight_type='nonlinear'`时使用
   - `alpha=1`: 等价于线性权重
   - `alpha=2`: 权重为绝对值的平方
   - `alpha>2`: 更大的差异获得更高的权重

## 使用示例

### 示例1: 禁用样本权重

```yaml
sample_weight:
  enabled: false
```

训练时所有样本权重相同（默认行为）。

### 示例2: 使用线性权重

```yaml
sample_weight:
  enabled: true
  weight_type: 'linear'
  eps: 0.01
```

权重直接与`price_diff`绝对值成正比。

### 示例3: 使用非线性权重（推荐）

```yaml
sample_weight:
  enabled: true
  weight_type: 'nonlinear'
  eps: 1e-6
  alpha: 2
```

权重为`price_diff`绝对值的平方，放大大差异样本的重要性。

### 示例4: 强化大差异样本

```yaml
sample_weight:
  enabled: true
  weight_type: 'nonlinear'
  eps: 1e-6
  alpha: 3
```

使用更高的`alpha`值，进一步增强大差异样本的权重。

## 工作原理

1. **特征工程阶段**：在`FeatureEngineer.compute_sample_weights()`中，根据训练集的`price_diff`列计算权重数组

2. **权重拆分**：在`ModelTrainer.hyperparameter_search()`中，权重数组会按照训练/验证比例自动拆分

3. **应用到训练**：
   - 超参数搜索：权重传递给XGBoost的`DMatrix`，影响每次试验的模型训练
   - 最终模型：权重传递给`fit()`方法的`sample_weight`参数

4. **训练输出**：训练过程中会显示权重统计信息（最小值、最大值、平均值）

## 权重效果对比

假设有以下样本：

| price_diff | 线性权重 (eps=0.01) | 非线性权重 (eps=0.01, alpha=2) |
|------------|-------------------|--------------------------------|
| 0.1        | 0.11              | 0.0121                         |
| 1.0        | 1.01              | 1.0201                         |
| 5.0        | 5.01              | 25.1001                        |
| 10.0       | 10.01             | 100.2001                       |

可以看到，非线性权重(alpha=2)会显著增加大差异样本的权重。

## 注意事项

1. **price_diff列必须存在**：样本权重功能依赖于训练数据中的`price_diff`列。如果该列不存在，会输出警告并禁用权重。

2. **数据规范性**：如果`price_diff`值域跨度很大，建议调整`eps`和`alpha`参数，避免权重差异过大导致训练不稳定。

3. **验证影响**：启用样本权重会影响验证集评估，因为验证集样本也会被赋予不同权重。

4. **性能考虑**：权重计算本身非常快速，对训练速度影响可忽略不计。

## 实验建议

1. **基线对比**：先用`enabled=false`训练一个基线模型
2. **线性尝试**：尝试`weight_type='linear'`，观察效果
3. **非线性调优**：如果效果好，尝试不同的`alpha`值（如1.5, 2, 2.5, 3）
4. **指标评估**：对比不同权重设置下的RMSE、符号一致性等指标

## 技术实现

相关代码文件：
- `utils.py`: `calculate_sample_weights()` - 权重计算核心函数
- `feature_engineering.py`: `compute_sample_weights()` - 特征工程中的权重计算接口
- `model_trainer.py`: 权重应用到超参数搜索和最终训练
- `main.py`: 训练流程中调用权重计算

## 更新日志

- 2025-10-28: 初始版本，支持线性和非线性权重，基于price_diff绝对值计算

