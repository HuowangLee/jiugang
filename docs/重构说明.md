# 训练脚本重构说明

## 📌 重构概览

原始的 `run_xgb_direct.py` (539行) 已被重构为模块化设计，包含：

- **1个配置文件** (`config.yaml`)
- **6个功能模块** (独立的Python文件)
- **1个主程序** (`main.py`)

## 🔄 代码迁移对照表

### 原始脚本 vs 新模块

| 原始代码行 | 功能 | 新位置 |
|-----------|------|--------|
| 30-68 | 可配置项（硬编码变量） | `config.yaml` |
| 74-88 | 工具函数 (safe_read_csv, quick_check_columns) | `utils.py` |
| 91-103 | 数据读取合并 | `data_loader.py` → `DataLoader.load_*_data()` |
| 105-122 | 创建时间索引 | `data_loader.py` → `DataLoader.make_datetime_index()` |
| 124-131 | 构建滞后特征 | `feature_engineering.py` → `FeatureEngineer.build_lag_features()` |
| 133-142 | 符号标签和一致性计算 | `utils.py` → `sign_label()`, `sign_agreement()` |
| 144-150 | 按日期计算一致性 | `utils.py` → `per_date_sign_agreement()` |
| 152-169 | 自定义评估函数 | `model_trainer.py` → `ModelTrainer.evaluate_trial()` |
| 176-234 | 主流程：数据准备 | `main.py` → `main()` 函数前半部分 |
| 236-346 | 超参数搜索循环 | `model_trainer.py` → `ModelTrainer.hyperparameter_search()` |
| 348-366 | 训练最终模型 | `model_trainer.py` → `ModelTrainer.train_final_model()` |
| 371-382 | KeyboardInterrupt 处理 | `model_trainer.py` 中集成 |
| 387-411 | 测试集预测和评估 | `evaluator.py` → `ModelEvaluator.predict()`, `calculate_metrics()` |
| 413-456 | 保存结果和参数 | `evaluator.py` → `ModelEvaluator.save_results()` |
| 458-499 | 可视化 | `evaluator.py` → `ModelEvaluator.visualize()` |
| 500-533 | 保存模型和特征重要性 | `evaluator.py` → `ModelEvaluator._save_model()`, `_save_feature_importance()` |

## 🎯 重构的主要改进

### 1. 配置管理

**之前：**
```python
# 硬编码在脚本顶部
DATA_FEAT_TRAIN = 'data_features_train.csv'
MODE = 'classification'
N_RANDOM_TRIALS = 100
```

**现在：**
```yaml
# config.yaml
data:
  feat_train: 'data_realtime/data_features_train.csv'
model:
  mode: 'classification'
  n_random_trials: 100
```

✅ **优势：** 无需修改代码，只需编辑配置文件

### 2. 数据加载

**之前：**
```python
def read_and_merge(feat_path, lab_path):
    # 混合在主流程中
    feat = safe_read_csv(feat_path)
    lab = safe_read_csv(lab_path)
    # ...
```

**现在：**
```python
# data_loader.py
class DataLoader:
    def load_train_data(self):
        return self._read_and_merge(...)
```

✅ **优势：** 封装为类，易于扩展和测试

### 3. 特征工程

**之前：**
```python
# 在主流程中混杂
concat_feats = pd.concat([train_df, test_df])
concat_lagged = build_lag_features(concat_feats, feat_cols, LAG_LENGTH)
```

**现在：**
```python
# feature_engineering.py
class FeatureEngineer:
    def process_train_test_features(self, train_df, test_df, feat_cols):
        # 完整的特征工程流程
```

✅ **优势：** 独立模块，清晰的接口

### 4. 模型训练

**之前：**
```python
# 在主流程中有一个超大的for循环
for i in trange(len(sampler)):
    params = sampler[i].copy()
    # 100+ 行代码
```

**现在：**
```python
# model_trainer.py
class ModelTrainer:
    def hyperparameter_search(self, X_train, y_train, feature_names):
        # 拆分为多个小方法
        self.split_train_validation()
        self.create_dmatrix()
        self.evaluate_trial()
```

✅ **优势：** 逻辑分层，每个方法职责单一

### 5. 结果评估

**之前：**
```python
# 散落在主流程末尾
res_df = pd.DataFrame({...})
per_date = per_date_sign_agreement(res_df)
# 保存各种文件...
plt.figure(...)
```

**现在：**
```python
# evaluator.py
class ModelEvaluator:
    def predict(self, model, X_test, y_test):
        # 预测
    def calculate_metrics(self, res_df):
        # 计算指标
    def save_results(self, ...):
        # 保存
    def visualize(self, res_df):
        # 可视化
```

✅ **优势：** 结构清晰，易于添加新的评估指标

## 📊 模块职责划分

```
┌─────────────────┐
│   main.py       │  ← 主流程协调
└────────┬────────┘
         │
    ┌────┴────┐
    ↓         ↓
┌─────────┐  ┌──────────────┐
│ config  │  │ data_loader  │
│ manager │  └──────┬───────┘
└─────────┘         │
                    ↓
              ┌──────────────┐
              │   feature    │
              │ engineering  │
              └──────┬───────┘
                     │
                     ↓
              ┌──────────────┐
              │    model     │
              │   trainer    │
              └──────┬───────┘
                     │
                     ↓
              ┌──────────────┐
              │  evaluator   │
              └──────────────┘
                     ↑
                     │
              ┌──────────────┐
              │    utils     │  ← 工具函数
              └──────────────┘
```

## 🔍 新旧对比

### 代码行数

| 项目 | 行数 |
|-----|-----|
| 原始脚本 | 539行（单文件） |
| 新架构总计 | ~1200行（多文件） |
| - config_manager.py | ~60行 |
| - utils.py | ~90行 |
| - data_loader.py | ~120行 |
| - feature_engineering.py | ~140行 |
| - model_trainer.py | ~300行 |
| - evaluator.py | ~290行 |
| - main.py | ~160行 |
| config.yaml | ~80行 |

💡 虽然总行数增加，但每个文件都很小，职责清晰

### 可维护性

| 方面 | 原始脚本 | 新架构 |
|-----|---------|--------|
| 修改参数 | 需要编辑代码 | 只需编辑配置文件 ✅ |
| 添加新功能 | 在长函数中插入 | 在相应模块中添加 ✅ |
| 单元测试 | 难以测试 | 每个类都可独立测试 ✅ |
| 代码复用 | 难以复用 | 可导入任意模块 ✅ |
| 团队协作 | 容易冲突 | 可并行开发不同模块 ✅ |

## 🚀 使用场景对比

### 场景1：修改超参数搜索次数

**之前：**
```python
# 需要编辑 run_xgb_direct.py
N_RANDOM_TRIALS = 50  # 第41行
```

**现在：**
```yaml
# 只需编辑 config.yaml
model:
  n_random_trials: 50
```

### 场景2：切换回归/分类

**之前：**
```python
# 需要编辑并注释/取消注释
# MODE = 'regression'
MODE = 'classification'  # 第39行
```

**现在：**
```yaml
model:
  mode: 'regression'  # 或 'classification'
```

### 场景3：运行不同配置

**之前：**
```bash
# 需要手动备份脚本
cp run_xgb_direct.py run_xgb_direct_backup.py
# 修改原脚本
# 运行
python run_xgb_direct.py
```

**现在：**
```bash
# 准备多个配置文件
python main.py --config config_test1.yaml
python main.py --config config_test2.yaml
python main.py --config config_test3.yaml
```

## 📝 迁移指南

如果你熟悉原始脚本，想要迁移到新架构：

### 1. 找到原来的配置

在 `run_xgb_direct.py` 第30-68行找到你的配置

### 2. 转换到 config.yaml

```python
# 原始配置
DATA_FEAT_TRAIN = 'my_data/train_feat.csv'
MODE = 'regression'
N_RANDOM_TRIALS = 200
LAG_LENGTH = 5
```

转换为：

```yaml
data:
  feat_train: 'my_data/train_feat.csv'
model:
  mode: 'regression'
  n_random_trials: 200
feature_engineering:
  lag_length: 5
```

### 3. 运行新脚本

```bash
python main.py
```

## 🎓 学习路径

如果想深入理解新架构，建议按以下顺序阅读：

1. **config.yaml** - 了解所有可配置项
2. **main.py** - 理解整体流程
3. **utils.py** - 熟悉工具函数
4. **data_loader.py** - 数据如何加载
5. **feature_engineering.py** - 特征如何构建
6. **model_trainer.py** - 模型如何训练
7. **evaluator.py** - 结果如何评估

## ✨ 扩展性示例

### 添加新的评估指标

**新架构中只需：**

```python
# 在 evaluator.py 的 calculate_metrics 方法中添加
def calculate_metrics(self, res_df: pd.DataFrame) -> dict:
    # ... 现有代码 ...
    
    # 添加新指标
    from sklearn.metrics import precision_score
    precision = precision_score(res_df['label'], res_df['pred'])
    metrics['precision'] = float(precision)
    
    return metrics
```

### 添加新的超参数

**只需编辑 config.yaml：**

```yaml
hyperparameters:
  classification:
    n_estimators: [100, 200, 300]
    max_depth: [3, 4, 6]
    min_child_weight: [1, 3, 5]  # 新增
    reg_alpha: [0, 0.1, 1.0]      # 新增
```

## 🎉 总结

重构后的架构具有以下优势：

1. ✅ **配置驱动** - 无需修改代码即可调整参数
2. ✅ **模块化** - 每个模块职责清晰，易于维护
3. ✅ **可扩展** - 轻松添加新功能
4. ✅ **可测试** - 每个模块都可独立测试
5. ✅ **易读性** - 代码结构清晰，注释完善
6. ✅ **灵活性** - 支持多配置文件，便于实验

原始脚本 `run_xgb_direct.py` 保留用于参考，但建议使用新架构进行训练。

