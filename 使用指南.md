# XGBoost 训练脚本使用指南

## 📋 目录

1. [环境设置](#环境设置)
2. [快速开始](#快速开始)
3. [配置文件说明](#配置文件说明)
4. [常用命令](#常用命令)
5. [文件说明](#文件说明)

---

## 环境设置

### 第一次使用

如果还没有创建conda环境：

```bash
# 创建环境
conda create -n jiugang python=3.10 -y

# 激活环境
conda activate jiugang

# 安装依赖
cd xgboost_trainer
pip install -r requirements.txt
```

### 后续使用

```bash
# 激活环境
conda activate jiugang
```

---

## 快速开始

### 1. 使用默认配置运行

```bash
cd xgboost_trainer
python main.py
```

### 2. 快速测试（10次试验）

```bash
python main.py --config config_quick_test.yaml
```

### 3. 运行回归任务

```bash
python main.py --config config_regression.yaml
```

---

## 配置文件说明

项目提供了三个配置文件示例：

| 配置文件 | 说明 | 用途 |
|---------|------|------|
| `config.yaml` | 默认配置（分类任务） | 正式训练使用 |
| `config_regression.yaml` | 回归任务配置 | 回归问题 |
| `config_quick_test.yaml` | 快速测试配置 | 调试和快速验证 |

### 主要配置项

```yaml
# 模式选择
model:
  mode: 'classification'  # 或 'regression'
  n_random_trials: 100    # 超参数搜索次数（越大越精确但越慢）

# 数据路径
data:
  feat_train: 'data_realtime/data_features_train.csv'
  label_train: 'data_realtime/data_label_train.csv'
  # ...

# 样本权重
sample_weight:
  enabled: true    # 是否启用非线性加权
  alpha: 2         # 权重指数
```

---

## 常用命令

### 查看帮助

```bash
python main.py --help
```

### 使用自定义配置

```bash
# 复制并修改配置文件
cp config.yaml my_config.yaml
# 编辑 my_config.yaml
# 运行
python main.py --config my_config.yaml
```

### 修改超参数搜索次数

编辑配置文件中的 `model.n_random_trials`：

- `10-20`：快速测试
- `50-100`：正常训练
- `200+`：精细搜索

### 切换数据源

修改配置文件中的 `data` 部分：

```yaml
data:
  feat_train: 'data_ahead/data_features_train.csv'  # 改为data_ahead
  label_train: 'data_ahead/data_label_train.csv'
  feat_test: 'data_ahead/test_df_feat.csv'
  label_test: 'data_ahead/test_df_lab.csv'
```

---

## 文件说明

### 核心文件

| 文件 | 说明 |
|------|------|
| `main.py` | 主程序入口 |
| `config.yaml` | 默认配置文件 |
| `requirements.txt` | Python依赖包 |
| `README.md` | 详细文档 |

### 模块文件

| 文件 | 功能 |
|------|------|
| `config_manager.py` | 配置管理 |
| `data_loader.py` | 数据加载 |
| `feature_engineering.py` | 特征工程 |
| `model_trainer.py` | 模型训练 |
| `evaluator.py` | 评估和可视化 |
| `utils.py` | 工具函数 |

### 旧文件

| 文件 | 说明 |
|------|------|
| `run_xgb_direct.py` | 原始训练脚本（已废弃，仅供参考） |

---

## 输出文件

训练完成后，会生成 `output-YYYYMMDD_HHMMSS/` 目录，包含：

```
output-20251026_143022/
├── params_json.json              # 训练参数
├── best_params.json              # 最佳超参数
├── eval_summary.json             # 评估摘要
├── per_date_sign_agreement.csv   # 按日期的一致性
├── test_predictions.csv          # 预测结果
├── feature_importance.csv        # 特征重要性
├── xgb_best_model.json           # 模型（JSON）
├── xgb_best_model.pkl            # 模型（PKL）
└── label_pred_line.png           # 可视化图表
```

---

## 常见问题

### Q1: 如何加快训练速度？

**方法1**：减少超参数搜索次数
```yaml
model:
  n_random_trials: 20  # 从100改为20
```

**方法2**：减少boosting轮数
```yaml
model:
  num_boost_round: 500  # 从1000改为500
```

**方法3**：使用快速测试配置
```bash
python main.py --config config_quick_test.yaml
```

### Q2: 如何查看训练进度？

训练过程中会显示进度条和每次试验的结果：

```
超参数试验: 23%|████▊            | 23/100 [02:15<07:32,  5.88s/it]
Trial 23 params={...} -> Agreement=0.75342, score=0.75342, rounds=145
  ✓ 发现更好的模型! score=0.75342
```

### Q3: 训练中断了怎么办？

按 `Ctrl+C` 会触发优雅退出，保存当前最佳模型。

### Q4: 如何对比不同配置？

1. 创建多个配置文件
2. 分别运行
3. 对比各自 output 目录中的 `eval_summary.json`

### Q5: 文件找不到怎么办？

检查配置文件中的数据路径是否正确：
```yaml
data:
  feat_train: 'data_realtime/data_features_train.csv'  # 确保路径正确
```

---

## 技巧和最佳实践

1. **第一次运行**：使用 `config_quick_test.yaml` 快速验证流程
2. **正式训练**：使用默认 `config.yaml`，设置合适的 `n_random_trials`
3. **实验对比**：为不同实验创建不同的配置文件，命名如 `config_exp1.yaml`
4. **记录结果**：保存 output 目录，重命名为有意义的名称
5. **特征调试**：检查 `feature_importance.csv` 了解哪些特征重要

---

## 示例工作流

```bash
# 1. 激活环境
conda activate jiugang

# 2. 进入目录
cd xgboost_trainer

# 3. 快速测试
python main.py --config config_quick_test.yaml

# 4. 查看结果
cd output-quick-test-*
cat eval_summary.json

# 5. 满意后正式训练
cd ..
python main.py

# 6. 查看最终结果
cd output-*
ls -lh
```

---

## 联系和支持

如有问题，请查看：
1. `README.md` - 详细文档
2. 各模块源代码 - 注释详细
3. 原始脚本 `run_xgb_direct.py` - 了解原理

---

**祝训练顺利！** 🚀

